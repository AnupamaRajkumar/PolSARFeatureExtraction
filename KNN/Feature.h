#pragma once
#ifndef FEATURE_H
#define FEATURE_H

//using namespace std;
//using namespace cv;

#if 0
class Feature {
public:
	void lexi2pauli(vector<Mat>& lexi, vector<Mat>& pauli);
	Mat RGBFeaturesFromPauli(vector<Mat>& lexi, vector<Mat>& pauli);
};

#endif

#endif
